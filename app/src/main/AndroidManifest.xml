<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jaeyoungkim.app.exercisehelper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.dailyexercise.DailyExerciseDetail"></activity>
        <activity android:name=".activity.routine.RoutineRegister03" />
        <activity android:name=".activity.routine.RoutineRegister02" />
        <activity android:name=".activity.DietRegister" />
        <activity android:name=".activity.routine.RoutineRegister01" />
        <activity android:name=".activity.dailyexercise.DailyExercise" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>